# Contributing to BurstGridGo

We welcome contributions! To get started, please make sure your changes pass all tests.

## Running Tests

To run the full test suite, including the race detector, use the following command:

```sh
make test
